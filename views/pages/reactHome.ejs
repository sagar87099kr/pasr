<% layout('layoutes/boilerplate') %>

<!-- React Root Element -->
<div id="react-homepage" data-is-logged-in="<%= typeof currUser !== 'undefined' && currUser ? 'true' : 'false' %>"></div>

<!-- React Bundle Assets (cache busted with current timestamp on render) -->
<link rel="stylesheet" href="/dist/assets/main.css?v=<%= Date.now() %>">
<script src="/dist/assets/main.js?v=<%= Date.now() %>" type="module" defer></script>

<style>
    /* Ensure the React container can take full width if needed */
    .react-home-container {
        width: 100%;
        max-width: 100vw;
        margin: 0;
        padding: 0;
    }
</style>
